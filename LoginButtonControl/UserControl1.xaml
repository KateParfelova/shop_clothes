<UserControl x:Class="LoginButtonControl.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginButtonControl"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="105"
             x:Name="root">

    <Button x:Name="loginButton" 
            Width="{Binding ButtonWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Height="{Binding ButtonHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Content="{Binding ButtonText, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Background="Black"
            Foreground="White"
            FontWeight="SemiBold"
            BorderThickness="0"
            Cursor="Hand"
            Click="LoginButton_Click">

        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="5"
                                    SnapsToDevicePixels="True">

                                <Border.Effect>
                                    <DropShadowEffect x:Name="shadow" BlurRadius="5" Opacity="0.3"/>
                                </Border.Effect>

                                <ContentPresenter x:Name="contentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="0"/>
                            </Border>

                            
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Button.Style>
    </Button>
</UserControl>