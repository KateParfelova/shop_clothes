<UserControl x:Class="RegistrationButtonControl.UserControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RegistrationButtonControl"
             mc:Ignorable="d" 
             d:DesignHeight="40"
             d:DesignWidth="105"
             x:Name="root2">
    <Button x:Name="regButton" 
         Width="{Binding ButtonWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
         Height="{Binding ButtonHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
         Content="{Binding ButtonText, RelativeSource={RelativeSource AncestorType=UserControl}}"
         Background="White"
         Foreground="Black"
         BorderBrush="Black"
         FontWeight="SemiBold"
         BorderThickness="2"
         Cursor="Hand"
         Click="LoginButton_Click">

        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                 Background="{TemplateBinding Background}"
                                 CornerRadius="5"
                                 SnapsToDevicePixels="True">

                                <Border.Effect>
                                    <DropShadowEffect x:Name="shadow" BlurRadius="5" Opacity="0.3"/>
                                </Border.Effect>

                                <ContentPresenter x:Name="contentPresenter"
                                               Content="{TemplateBinding Content}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="0"/>
                            </Border>


                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Button.Style>
    </Button>
</UserControl>
